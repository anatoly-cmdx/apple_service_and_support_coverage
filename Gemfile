source 'https://rubygems.org'

gem 'nokogiri'

# requires qt5-default libqt5webkit5-dev on Ubuntu
# requires JavaScript runtime (nodejs, therubyracer)
gem 'capybara'
gem 'capybara-webkit'

# requires xvfb on Ubuntu
gem 'headless'

group :test do
  gem 'rspec'
  gem 'shoulda-matchers'
  gem 'json-schema'
end
